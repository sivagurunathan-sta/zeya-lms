# Backend
PORT=3000

# MongoDB (use MONGODB_URI; in dev, the app falls back to in-memory MongoDB if unset)
MONGODB_URI=
MONGODB_DB_NAME=student_lms

# JWT
JWT_SECRET=
JWT_REFRESH_SECRET=

# Frontend URL (for emails and CORS)
FRONTEND_URL=http://localhost:3000

# SMTP settings (leave empty in dev to disable and use JSON transport)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
EMAIL_FROM_NAME=Student LMS

# AWS (required if S3 uploads are used)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=

# Payments (if used)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Frontend
REACT_APP_API_URL=http://localhost:3000/api/auth/login \
